const container = document.querySelectorAll('.container');


for (let i =0 ;i < 30; i++){
    const newColorContainerEl = document.createElement('div'); //creating the new element of div
    newColorContainerEl.classList.add('color-container'); // adding the 'color-container' class into the created element
    container.appendChild(newColorEl); // adding the new div element with its 'color-container' class into the container parent

} // creating 30 newColorContainers 




